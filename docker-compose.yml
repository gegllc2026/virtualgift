services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: gateway
      POSTGRES_PASSWORD: gateway
      POSTGRES_DB: gateway_gifting
    ports:
      - "5432:5432"
    volumes:
      - gateway_pg:/var/lib/postgresql/data

volumes:
  gateway_pg:
